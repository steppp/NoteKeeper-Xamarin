<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:system="clr-namespace:System;assembly=netstandard"
             x:Class="NoteKeeper.Views.ItemDetailPage"
             Title="{Binding Title}">

    <!--
        ContentPage.ToolbarItems can be used in any ContentPage to manage the toolbar
    -->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="CancelMenuItem_OnClicked"/>
        <ToolbarItem Text="Save" Clicked="SaveMenuItem_OnClicked"/>
    </ContentPage.ToolbarItems>
 
    <StackLayout Spacing="20" Padding="15">
        <Picker x:Name="NoteCourse" Title="Note course" FontSize="Medium">
            <Picker.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Course 1</x:String>
                    <x:String>Course 2</x:String>
                    <x:String>Course 3</x:String>
                </x:Array>
            </Picker.ItemsSource>
        </Picker>

        <Entry x:Name="NoteHeading" Placeholder="Note heading" FontSize="Medium" />

        <Editor x:Name="NoteText" Placeholder="Note" AutoSize="TextChanges" FontSize="Medium"></Editor>
    </StackLayout>
    
</ContentPage>